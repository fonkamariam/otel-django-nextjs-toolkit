apiVersion: 1

providers:
  - name: 'Node Exporter Hosts'               # Friendly name in Grafana
    orgId: 1
    folder: 'Hosts'                           # Dashboard folder in Grafana UI
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards/node-exporter
      # homeDashboardId: 1860               # Optional: make this the home dashboard